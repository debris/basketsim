table! {
    players (id) {
        id -> Integer,
        first_name -> Text,
        last_name -> Text,
        age -> Unsigned<Tinyint>,
        height -> Unsigned<Tinyint>,
        weight -> Unsigned<Tinyint>,
    }
}
